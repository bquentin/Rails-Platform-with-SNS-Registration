Rails3-Platform-Registration-via-Facebook-Twitter-Google
========================================================

Basic Rails3 platform which let users register via their SNS credentials (Oauth) / creates new users based on the SNS info